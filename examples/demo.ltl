// examples/demo.ltl — runnable demo for ltl_elf_read

import std::io
use ltl_elf_read as lib

fn main() {
    io::println("ltl_elf_read demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}