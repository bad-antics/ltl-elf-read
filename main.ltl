// ltl-elf-read/main.ltl
//
// ELF64 parser with section, symbol, and dynamic-link tables.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-elf-read v0.1.0")
    io::println("ELF64 parser with section, symbol, and dynamic-link tables.")
    0
}
